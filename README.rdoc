== README

This is a sample rails app using react for UI.
To run the app simply run:
<code>
	bundle install && rake db:migrate
</code>
<code>
	rails server
</code>

The meat of the react code is located in *app/assets/javascripts/components/posts.js.jsx*.